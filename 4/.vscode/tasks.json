{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build Current",
            "type": "shell",
            "command": "mkdir -p build && cd build && cmake .. && make",
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "build"
        }
    ]
}